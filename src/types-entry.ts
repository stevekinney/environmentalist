/** Types-only entry for consumers that must not import the runtime resolver. */
export type {
  CamelCasedPropertiesDeep,
  ConfigLoader,
  DeepPartial,
  DotenvOptions,
  EnvironmentalistOptions,
  Environment,
  Provenance,
  ResolutionTrace,
  SafeResult,
  Source,
  SourceContext,
  SourceMap,
  SourceName,
  SourceResult,
  SourceSpec,
} from './types.js';
export type {
  EnvironmentChange,
  EnvironmentChangeEvent,
  WatchOptions,
  Watcher,
} from './watch/watcher.js';
